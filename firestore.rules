rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Google 로그인 사용자는 자신의 UID 아래 데이터만 읽고 쓸 수 있습니다.
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null
        && request.auth.uid == userId;
    }
  }
}
